# Hardware Mapping (Verify with 'lsblk' on the Pi that the USB is sda!)
usb_device: "/dev/sda"
usb_crypt_name: "usb_crypt"

# Target Proxmox VM Data
media_vm_ip: "192.168.83.3"
backup_user: "pi-backup-puller"
